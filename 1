#include <stdio.h>
#define LEN 10
int length(int a[], int j)
{
	int j = 0;
	while (j < LEN)
	{
		if (a[j] == 0)
			break;
		else j++;
	}
	return j;
	printf("有效元素的个数为%d", j);
}
int main(void)
{
	int a[LEN] = { 3,6,9,13,15 };
	length(a);
}
